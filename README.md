# Dotfiles

## To install

### Neovim

```bash
curl -LO https://github.com/neovim/neovim/releases/download/v0.12.3/nvim-linux-x86_64.appimage -o /tmp/nvim 
chmod +x /tmp/nvim 
sudo cp /tmp/nvim /usr/local/nvim
```
